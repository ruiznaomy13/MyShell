#include <minishell.h>

int main(int ac, char **av, char **env)
{
    t_all   *all;

    (0 || (ac = 0) || (av = 0) || (env = 0));
    while (42)
    {
        all->line = readline("myshell🌞> ");
    }
}